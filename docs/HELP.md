# unframed 玩家手册

## 什么是 unframed？

unframed 是一个 AI 驱动的叙事游戏。没有预设的游戏机制——AI 从零开始构建世界观、规则、角色与剧情。你所做的一切都会影响故事走向。

## 快速开始

```bash
# 启动 TUI（默认）
unframed

# CLI 模式
unframed --cli

# 跳过菜单直接加载种子
unframed --seed seeds/xxx.md

# 继续上次游戏（CLI 模式）
unframed --cli --continue
```

## 界面模式

unframed 提供两种界面：TUI（终端图形界面）和 CLI（命令行界面）。默认启动 TUI，使用 `--cli` 切换到 CLI 模式。

---

### TUI 模式

```
┌──────────────────────────────────────────────────┐
│  U N F R A M E D                      14:30:21   │
├────────────────────────────────┬─────────────────┤
│                                │ ## 角色状态      │
│  叙事区域                       │ - **生命值**: 85 │
│  （AI 的故事输出）               │ - **位置**: 断指  │
│                                │   酒吧           │
│                                │                  │
│                                │ ---              │
│                                │ 回合 12          │
├────────────────────────────────┴─────────────────┤
│  AI 正在构思剧情...                                │
├──────────────────────────────────────────────────┤
│  > 输入你的行动...                        [发送]  │
└──────────────────────────────────────────────────┘
```

- **左侧**：叙事输出区，AI 生成的故事以 Markdown 渲染
- **右侧**：角色状态面板，AI 用 `show_var` 工具控制的可见变量
- **底部**：输入框，输入你的行动或对话
- **状态栏**：AI 处理时显示当前操作（如"正在修改世界状态"）

#### TUI 启动菜单

| 选项 | 说明 |
|------|------|
| **新建存档** | 输入存档名 → 选择种子 → 开始新游戏 |
| **继续上一个存档** | 直接回到上次玩的存档（仅在有历史时显示） |
| **加载存档** | 从已有存档中选择一个加载 |
| **存档管理** | 查看、重命名、删除已有存档 |
| **设置** | 配置 API Key、模型、温度等，设置将持久保存 |
| **文档** | 查看此帮助的概要 |
| **退出** | 退出游戏 |

#### TUI 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息 |
| `Ctrl+S` | 打开存档管理器（Enter=保存，Delete=删除，R=重命名） |
| `Ctrl+L` | 打开读档列表 |
| `Ctrl+D` | 打开删档列表 |
| `Escape` | 自动存档并返回启动菜单 |

---

### CLI 模式

启动菜单：

| 选项 | 说明 |
|------|------|
| **新建存档** | 选择一个种子 → 开始新游戏 |
| **继续上一个存档** | 直接回到上次玩的存档（仅在有历史时显示） |
| **加载存档** | 从已有存档中选择一个加载 |
| **帮助文档** | 查看此帮助 |
| **退出** | 退出游戏 |

游戏中命令：

| 命令 | 说明 |
|------|------|
| `/save` | 打开存档菜单（输入存档名） |
| `/save 我的存档` | 快速保存到指定名称 |
| `/save list` | 列出所有存档 |
| `/load` | 打开读档菜单 |
| `/load 我的存档` | 快速加载指定存档 |
| `/load list` | 列出所有存档 |
| `/delete` | 打开删档菜单 |
| `/quit` | 退出游戏 |

---

## 种子（Seed）

种子是游戏的"剧本文件"，定义了世界观、时间、地点、角色、规则和目标。每个种子由一对文件组成：`seeds/<名称>.json`（标题索引）和 `seeds/<名称>.md`（剧本内容）。

内置种子：

```
赛博朋克：深渊回响  — 2087 年，永夜之城"新重庆"
潮汐监狱           — 2147 年，深海环形海上堡垒
锈蚀边境：绿洲协议  — 2147 年，核冬天后的废土求生
孤星：坠落日       — 2247 年，殖民船坠毁后的外星求生
霓虹裁决           — 2140 年，同步轨道升降梯内
循环日             — 被困在同一天的时间循环
sudo apt 求生记    — 服务器崩溃后的命令行求生
地下王座           — 地底世界的权力游戏
```

你也可以自己编写种子——参考 `docs/SEED_SPEC.md`。

## 游戏机制

### AI 是唯一的创世者

没有预设的规则、属性或战斗系统。AI 每轮先构思剧情方向，更新所有游戏状态变量，检查一致性，然后输出叙事。

### 状态即真相

游戏世界的所有状态都由 AI 通过工具调用读写。AI 的叙事必须与状态一致。右侧面板显示的是 AI 标记为"对玩家可见"的变量。

### 变量分层

AI 看到的变量分为三层：
- **核心区**：常驻变量，始终可见（最多 10 个，如玩家目标）
- **活跃区**：最近操作过的变量（最多 20 个）
- **目录区**：其他变量，仅显示名称

### 剧情规划

AI 会构建一棵剧情树，提前规划可能的走向。被跳过的分支自动修剪。当故事到达结局节点时结束。

### 子 AI 系统（NPC）

AI 可以创建和管理长期存在的子 AI，作为游戏世界中的 NPC、同伴或组织。这些子 AI 拥有独立记忆，能记住每一次对话，扮演指定的角色性格。

- **创建角色**：AI 调用 `spawn_agent` 创建一个持久 NPC，赋予其名称、性格和上下文
- **与角色对话**：AI 通过 `call_agent` 与已有 NPC 交流，NPC 基于自己的记忆和性格回复
- **移除角色**：AI 通过 `terminate_agent` 终止 NPC，其记忆随之消失

子 AI 可以读取游戏变量了解世界状态，但不能直接修改它们。玩家通过主 AI 的叙事间接与 NPC 互动。

## 存档

- 存档文件存储在 `~/.unframed/saves/<uuid>.json`，以 UUID 命名
- **TUI 模式**：每轮自动保存到当前命名的存档
- **CLI 模式**：每轮自动保存到 `~/.unframed/autosave.json`
- 主菜单"继续上一个存档"可直接回到上次游戏
- 存档文件存 JSON，包含：回合数、变量、对话历史、剧情树

## 配置

配置文件 `~/.unframed/config.json` 自动加载（如不存在则用默认值）。你可以在 TUI 的"设置"菜单中修改并持久保存，或直接编辑该文件。

读取优先级：**CLI 参数 > 环境变量 > 配置文件 > 内置默认**

```bash
export OPENAI_API_KEY="sk-..."
export OPENAI_BASE_URL="https://api.example.com/v1"
export OPENAI_MODEL="gpt-4o"
export OPENAI_TEMPERATURE="0.7"
```

## 给玩家的建议

- **主动行动**：直接描述你的行动，AI 会根据你的选择推进剧情
- **探索世界**：可以随时询问环境信息（"这附近有什么？"）
- **没有预设规则**：你觉得合理的事情都可以尝试
- **改变方向**：想换个玩法直接说（"其实我是..."）
- **保存经常**：TUI 用 `Ctrl+S`，CLI 用 `/save` 定期保存
